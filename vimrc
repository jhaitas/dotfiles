" Prevents vim from emulating the original vi's bugs and limitations
set nocompatible

set smartindent
set tabstop=4 "sets 4-space tabs
set expandtab "turn tabs into whitespace
set shiftwidth=4 "indent width for autoindent

set ruler "statusline displays the current cursor position

set incsearch "search for text as it is entered
set hlsearch "highlight search matches

" set colors
set t_Co=256
syntax enable "syntax highlighting

